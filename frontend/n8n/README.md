# INSURA n8n Workflow Images

Place the n8n workflow screenshots in this folder with these names:

- `insura-policy-extraction-workflow.png`
- `insura-policy-comparison-workflow.png`
- `insura-learning-generation-workflow.png`

The repository README links to these PNG files so the workflows can be viewed directly without importing a JSON export into n8n.